import { Text, View } from 'react-native';

export default function NewActivityScreen({navigation}) {
    return (
      <View style={{flex:1}}>
        <Text>
            Work In Progress: New activity Screen
        </Text>
      </View>
    );
  }
  
